/**
 * Count the number of conversions based on the provided formula.
 * @param {number} probotCredits - The number of probot credits.
 * @returns {number} - The propal amount calculated using the formula.
 */
function countConversions(probotCredits) {
    return probotCredits / 20;
  }
  
module.exports = countConversions;
  
// Developed by AmtiXDev
// Discord Username: amtix